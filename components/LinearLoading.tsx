import { Box, LinearProgress } from "@mui/material";

const LinearLoading = () => {
    return (
        <Box sx={{ width: '100%' }}>
            <LinearProgress />
        </Box>
    );
};

export default LinearLoading;
